export interface ILoginModel {
    username: string;
    role: string;
    originalUserName: string;
    accessToken: string;
    refreshToken: string;
}
export interface IApplicationUser {
    username: string;
    role: string;
    originalUserName: string;
}
